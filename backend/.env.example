# MongoDB Atlas connection string (optional — falls back to JSON file storage if not set)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/

# Port (optional, defaults to 4000)
PORT=4000

# Frontend URL for CORS (optional, Render URLs are allowed automatically)
# FRONTEND_URL=https://your-frontend.onrender.com
